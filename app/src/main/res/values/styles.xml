<resources>

    <!-- No action bar application theme. -->
    <style name="NoActionBarHayTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- Action bar application theme. -->
    <style name="ActionBarHayTheme" parent="Theme.AppCompat">

    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light" />

</resources>
